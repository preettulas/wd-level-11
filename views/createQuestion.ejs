<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>New question</title>
</head>

<body class="bg-gray-200">
  <div class="bg-grey-lighter min-h-screen flex flex-col">
    <div class="container max-w-sm mx-auto flex-1 flex flex-col items-center justify-center   px-2">
      <div class="bg-white px-6 py-8 rounded shadow-md text-black w-full">
        <h1 class="mb-8 text-3xl text-left"> Add Question</h1>
          <form action="/createQuestion/<%= id %>" method="post">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="py-3">
           <label for="questionName" class="block mt-3 font-semibold">Enter Question:</label>
           <input id="questionName" name="questionName" type="text" placeholder="Question" class="block border border-gray-light w-full p-2 rounded mb-3">
        </div>  
        <div class="py-3"> 
           <label for="desc">Description:</label>
           <input id="desc" name="desc" type="text" placeholder="Description" class="block border border-gray-light w-full p-2 rounded mb-3">
        </div>  
        <div class="py-3"> 
           <button type="submit" class="text-white bg-green-600 hover:bg-green-900 font-medium rounded px-5 py-2">Create</button>        
        </div>  
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
            <% for(var i=0; i<messages.error.length; i++) { %>
              <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
            <% } %>
          <% } %>
        </div>
    </div>
  </body>
</html>
